class LightsController < ApplicationController
  # def create
  #   @light = Light.new(light_params)
  #   @light.save
  #   redirect_to towa_path(@light.meaning.towa)
  # end

  # private
  #   def light_params
  #     params.require(:light).permit(:score).merge(meaning_id: params[meaning_id], user_id: current_user.id)
  #   end
end
